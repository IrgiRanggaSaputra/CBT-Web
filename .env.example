# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=cbt_lpk
DB_PORT=3306

# Base URL (opsional - akan auto-generate jika tidak diset)
# BASE_URL=https://your-domain.com/

# Firebase Configuration (opsional - jika menggunakan Firebase)
# FIREBASE_KEY_PATH=/config/firebase-key.json
# atau encode firebase-key.json menjadi base64 dan gunakan:
# FIREBASE_KEY_JSON=base64-encoded-firebase-key
